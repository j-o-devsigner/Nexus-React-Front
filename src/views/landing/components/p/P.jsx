import styled from "styled-components";

const P = styled.p`
    font-family: var(--font-family);
    font-weight: 400;
    font-size: 20px;
    line-height: 28px;
    color: var(--color-text);
    text-align: justify;
`;

export default P